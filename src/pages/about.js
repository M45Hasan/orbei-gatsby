import * as  React from "react";
import Layout from "../components/Layout";

const about = () => {
  return (
    <Layout>
      <div>about</div>
    </Layout>
  );
};

export default about;
export const Head = () => (
  <>
    <title>about</title>
  </>
);
